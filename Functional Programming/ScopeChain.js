/*Scope Chain : Whenever our code tries to access a varibale during the function call , it starts the searching from local variable . And if the variable is not found m it'll continue searching in tis outer scope or parents function's scope until it reaches the global scope and completes searching for the variable there */